package sec06.exam04.package2;

import sec06.exam04.package1.A;

public class C {

	// 필드
	A a1 = new A(true);
	// A a2 = new A(1); A 클래스의 default 생성자는 접근 불가하다. (패키지 다름)
	// A a3 = new A("문자열"); A 클래스의 private 생성자는 접근 불가하다.
	
}
